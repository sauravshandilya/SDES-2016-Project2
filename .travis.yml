language: python

python:
  - "2.7"

sudo: required

before_install:
  - pip install coverage
  - pip install python-coveralls
  - pip install nose
  - pip install mock
  
script: python setup.py develop && nosetests -w tests

after_success:
  - coveralls

